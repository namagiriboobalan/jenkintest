pipeline{
	agent none
		stages {
				stage('Stage-1') {
			agent {label 'agent1'}
				steps {
					sh 'touch agent1'
                  			}
                     			}
                    	}
                        }
